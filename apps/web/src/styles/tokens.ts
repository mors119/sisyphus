/**
 * Serialized brand values for APIs and native controls that require a hex value.
 * Visual styling must use the semantic CSS tokens from styles/index.css.
 */
export const BRAND_ACCENT_HEX = '#ffcd49' as const;
