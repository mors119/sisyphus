package com.sisyphus.backend.__name__.repository;

import com.sisyphus.backend.__name__.entity.__Name__;
import org.springframework.data.jpa.repository.JpaRepository;

// JPA 기본 CRUD 리포지토리
public interface __Name__Repository extends JpaRepository<__Name__, Long> {
}